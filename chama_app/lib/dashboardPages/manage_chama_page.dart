import 'package:flutter/material.dart';

class ManageChamaPage extends StatelessWidget {
  const ManageChamaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Manage your Chama',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
